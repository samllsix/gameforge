{
    "patterns": [
        {
            "name": "Singleton Manager",
            "description": "游戏管理器单例模式",
            "example": "public class GameManager : MonoBehaviour {\n    public static GameManager Instance { get; private set; }\n    private void Awake() {\n        if (Instance == null) { Instance = this; DontDestroyOnLoad(gameObject); }\n        else { Destroy(gameObject); }\n    }\n}",
            "use_case": "GameManager, AudioManager, LevelManager"
        },
        {
            "name": "Object Pool",
            "description": "对象池模式，避免频繁Instantiate/Destroy",
            "example": "public class ObjectPool {\n    private Queue<GameObject> pool = new Queue<GameObject>();\n    public GameObject Get() { return pool.Count > 0 ? pool.Dequeue() : CreateNew(); }\n    public void Return(GameObject obj) { obj.SetActive(false); pool.Enqueue(obj); }\n}",
            "use_case": "子弹、敌人、特效等频繁创建销毁的对象"
        },
        {
            "name": "State Machine",
            "description": "有限状态机，管理角色或游戏状态",
            "example": "public enum PlayerState { Idle, Running, Jumping, Falling }\npublic class PlayerStateMachine {\n    private PlayerState currentState;\n    public void ChangeState(PlayerState newState) {\n        ExitState(currentState);\n        currentState = newState;\n        EnterState(newState);\n    }\n}",
            "use_case": "角色状态、游戏流程、AI行为"
        },
        {
            "name": "Event System",
            "description": "事件驱动架构，解耦模块通信",
            "example": "public static class GameEvents {\n    public static event System.Action<int> OnScoreChanged;\n    public static void ScoreChanged(int score) => OnScoreChanged?.Invoke(score);\n}",
            "use_case": "UI更新、成就系统、音效触发"
        },
        {
            "name": "Component Composition",
            "description": "组合优于继承，通过组件组合功能",
            "example": "public class Entity : MonoBehaviour {\n    private Health health;\n    private Movement movement;\n    private Combat combat;\n    private void Awake() {\n        health = GetComponent<Health>();\n        movement = GetComponent<Movement>();\n        combat = GetComponent<Combat>();\n    }\n}",
            "use_case": "角色系统、敌人类型、可交互物体"
        }
    ],
    "common_errors": [
        {
            "error": "NullReferenceException",
            "cause": "对象未初始化或已销毁",
            "solution": "使用GetComponent前检查null，或在Awake中初始化"
        },
        {
            "error": "MissingReferenceException",
            "cause": "引用的对象已被销毁",
            "solution": "在访问前检查对象是否有效"
        },
        {
            "error": "InvalidOperationException: Collection was modified",
            "cause": "遍历集合时修改了集合",
            "solution": "使用ToList()复制或反向遍历"
        }
    ]
}
